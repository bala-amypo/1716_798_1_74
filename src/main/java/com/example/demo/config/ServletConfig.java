package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ServletConfig {
    // This class can be used for advanced Servlet registration beans if needed.
    // For now, @ServletComponentScan in DemoApplication handles the SimpleStatusServlet.
}